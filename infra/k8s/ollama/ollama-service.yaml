apiVersion: v1
kind: Service
metadata:
  name: ollama-service
  namespace: llm-platform-prod
  labels:
    app: ollama
spec:
  type: ClusterIP
  ports:
    - port: 11434
      targetPort: 11434
      name: http
  selector:
    app: ollama
