apiVersion: v1
kind: Service
metadata:
  name: qdrant-service
  namespace: llm-platform-prod
  labels:
    app: qdrant
spec:
  type: ClusterIP
  ports:
    - port: 6333
      targetPort: 6333
      name: http
    - port: 6334
      targetPort: 6334
      name: grpc
  selector:
    app: qdrant
